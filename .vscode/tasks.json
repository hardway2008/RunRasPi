{
    "tasks": [
        {
            "type": "shell",
            "label": "build before run",
            // "command": "make clean",
            "command": "make",
            "args": [
                "x86"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}